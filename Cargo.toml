[workspace]
members = [
    "lindera-crf",
    "lindera-dictionary",
    "lindera-trainer",
    "lindera-cc-cedict",
    "lindera-jieba",
    "lindera-ipadic",
    "lindera-ipadic-neologd",
    "lindera-ko-dic",
    "lindera-unidic",
    "lindera",
    "lindera-analysis",
    "lindera-cli",
    "lindera-binding-core",
    "lindera-python",
    "lindera-nodejs",
    "lindera-ruby",
    "lindera-php",
    "lindera-wasm",
]
resolver = "2"

[workspace.package]
version = "5.2.0"
edition = "2024"
description = "A morphological analysis libraries and command line interface."
documentation = "https://docs.rs/lindera"
homepage = "https://github.com/lindera/lindera"
repository = "https://github.com/lindera/lindera"
readme = "README.md"
keywords = ["morphological", "analysis", "library", "dictionary", "cli"]
categories = ["text-processing"]
license = "MIT"

[workspace.dependencies]
anyhow = "1.0.104"
byteorder = "1.5.0"
csv = "1.4.0"
# Pinned exactly: the runtime walks crawdad's serialized byte layout in
# place (lindera-dictionary/src/dictionary/prefix_dictionary.rs), which the
# crate does not document as a stable contract. A round-trip test fails
# loudly if the layout changes; review it before moving this pin.
crawdad = "=0.3.0"
daachorse = "4.0.0"
lindera = { version = "5.2.0", path = "lindera" }
lindera-analysis = { version = "5.2.0", path = "lindera-analysis" }
lindera-binding-core = { version = "5.2.0", path = "lindera-binding-core" }
lindera-crf = { version = "5.2.0", path = "lindera-crf" }
lindera-cc-cedict = { version = "5.2.0", path = "lindera-cc-cedict" }
lindera-cli = { version = "5.2.0", path = "lindera-cli" }
lindera-dictionary = { version = "5.2.0", path = "lindera-dictionary" }
lindera-trainer = { version = "5.2.0", path = "lindera-trainer" }
lindera-ipadic = { version = "5.2.0", path = "lindera-ipadic" }
lindera-ipadic-neologd = { version = "5.2.0", path = "lindera-ipadic-neologd" }
lindera-jieba = { version = "5.2.0", path = "lindera-jieba" }
lindera-ko-dic = { version = "5.2.0", path = "lindera-ko-dic" }
lindera-nodejs = { version = "5.2.0", path = "lindera-nodejs" }
lindera-python = { version = "5.2.0", path = "lindera-python" }
lindera-ruby = { version = "5.2.0", path = "lindera-ruby" }
lindera-unidic = { version = "5.2.0", path = "lindera-unidic" }
lindera-php = { version = "5.2.0", path = "lindera-php" }
lindera-wasm = { version = "5.2.0", path = "lindera-wasm" }
log = "0.4.33"
num_cpus = "1.17.0"
once_cell = "1.21.4"
regex = "1.13.1"
rkyv = { version = "0.8.17", features = ["bytecheck"] }
serde = { version = "1.0.229", features = ["derive"] }
serde_json = "1.0.151"
strum = { version = "0.28.0", features = ["derive"] }
strum_macros = "0.28.0"

[profile.release]
lto = true

# Make sure that the build scripts and proc-macros are compiled with
# all the optimizations.
[profile.dev.build-override]
opt-level = 3
[profile.release.build-override]
opt-level = 3
[profile.bench.build-override]
opt-level = 3
[profile.test.build-override]
opt-level = 3

[profile.release.package.lindera-wasm]
opt-level = "s"
